<!-- Regenerated by scripts/generate_data_quality_doc.py — edit that file, not this one. -->
# Data quality

_Regenerated 2026-08-21 03:32 UTC by `scripts/generate_data_quality_doc.py`, which runs the four checks below as libraries and reports exactly what each one found — nothing here is hand-written or summarised from memory._

**Overall: PASSING** — 0 error(s) across all four checks, 2 item(s) flagged for review (flags don't fail the build; see each section for what they mean).

| Check | Status |
|---|---|
| Validate | PASSED, clean |
| Audit | PASSED, clean |
| Reconcile | PASSED, 2 item(s) flagged for review |
| Snapshot | PASSED, clean |

---

## Validate — envelope, provenance, crosswalk, normalise.py rules

`make validate` — PASSED, clean

<details><summary>Full log</summary>

```
CS Migration Compass — data validation

· curated data
  15 countries, 73 cities
  0 salary inversions, 15 explicit nulls (nulls are expected and fine)
· staleness
  0 record(s) past the prices/salaries staleness rule
· processed datasets + provenance
  52 datasets — 49 with data, 3 recorded as blocked/unavailable
· forecast integrity
  4 institutional forecast dataset(s)
· ODbL isolation — the Stack Overflow survey must never reach core.json
· survey pay and advertised pay never share a field
· postings.json and wage_distribution.json share no distinctive field
· percentile fields and prose are never labelled as seniority bands
· crosswalk comparison rule never reports a depth deeper than either side supports, and its resolved shared_key is the real agreed-upon truncation
  1081 mapping pairs checked, 0 violation(s)
· every crosswalk mapping carries a real, checked-against note
  47 mappings, 0 without a real note
· 'no-series' salary records carry a status, a reason, and dated or explicitly-absent data; meta and data agree on it
  2 no-data record(s) found and checked
· sources with no percentiles say so explicitly (a distribution flag from a known vocabulary)
  143 occupation record(s) checked
· normalise.py never substitutes a different year's FX rate than the one requested
  (15 committed wage_distribution.json rows scanned for FX-year mismatch: 0 found)
  0 violation(s)
· normalise.py never annualises an hourly figure without a real, sourced hours record
  0 violation(s)
· normalise.py's monthly->annual multiplier is exactly 12, nowhere else in the codebase
  0 violation(s)
· normalise.py's comparison_basis() never compares sources on incompatible bases
  0 violation(s)
· normalise.py performs no file writes — native processed data is never overwritten by it
  0 violation(s)
· every normalise.py conversion names its own rate, year and source
  0 violation(s)


PASSED — curated data, processed datasets and provenance are consistent.
```
</details>

---

## Audit — Tier 1 structural invariants

`make audit` — PASSED, clean

<details><summary>Full log</summary>

```
CS Migration Compass — data audit (Tier 1 structural invariants)
(complements `make validate` — see this file's own docstring for the split)

· percentiles are monotonic (p10<=p25<=median<=p75<=p90) wherever they co-occur
  416 distribution(s) with >=2 ordered points checked, 0 inversion(s)
· mean sits inside its own [p10, p90]
  85 distribution(s) with mean+p10+p90 checked, 0 out of range
· no negative pay figures; a real $0/€0/etc. is never presented as a figure
  1723 pay figure(s) checked, 0 negative-or-zero
· a 'central-tendency-only'/'mean-only' distribution label is never contradicted by real percentile fields
  19 labelled-central-tendency-only record(s) checked, 0 contradicted by their own data
· every pay field's unit (currency+period) is recoverable from its name or its container
  2911 pay field(s) checked, 0 with no recoverable unit
· order-of-magnitude plausibility per (currency, period), bounds built from this dataset's own spread
  903 figure(s) across 13 (currency, period) bucket(s), 0 flagged for review
· a native record's own embedded cross-check (e.g. Denmark's mdrsnit_check) reconciles under independent recomputation
  1 embedded cross-check(s) found and independently recomputed, 0 violation(s)
· every source is checked against its own expected refresh interval
  53 source(s) checked, 0 past their own expected refresh interval


PASSED — every processed dataset's own structural invariants hold (or are explicitly flagged for review).
```
</details>

---

## Reconcile — Tier 2, live sources + independent arithmetic

`python scripts/reconcile.py` — PASSED, 2 item(s) flagged for review

**2 flag(s) for review:**
- salary_de: structure and vintage reconciled live, but the wage VALUES were NOT compared. GENESIS's data/table serves all 1,300 occupation rows and timed out at 90/150/180/300/600s this session (narrowing via classifyingvariable1=KB10A5 does not reduce its build time, and the async job service is refused for this token, Code 12); each abandoned attempt also holds one of the account's 3 parallel slots for 15 minutes, which would block src_salary_de.py's own harvest. Germany is NOT counted among the value-reconciled sources.
- bls_oews: live reconciliation SKIPPED — no BLS_API_KEY in the environment. BLS's unregistered OEWS API caps at 25 requests/day and src_bls_oews.py spends 9-11 of them per run, so no request was attempted rather than risking the harvester's own quota (NEEDS-DECISION.md #13). Not counted as reconciled.

<details><summary>Full log</summary>

```
CS Migration Compass — reconciliation against live sources (Tier 2)

· Part A — live sample reconciliation (5 records or fewer per source, same each run)
    [salary_dk] reconciliation cache hit (data\raw\_reconcile_cache\salary_dk.json, <14d old)
    [salary_dk] 2 live headline figure(s) reconciled (STAND=408.56, MDRSNIT=65504.17)
    [salary_se] reconciliation cache hit (data\raw\_reconcile_cache\salary_se.json, <14d old)
    [salary_se] 24 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_nl] reconciliation cache hit (data\raw\_reconcile_cache\salary_nl.json, <14d old)
    [salary_nl] 4 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_no] reconciliation cache hit (data\raw\_reconcile_cache\salary_no.json, <14d old)
    [salary_no] 20 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_fi] reconciliation cache hit (data\raw\_reconcile_cache\salary_fi.json, <14d old)
    [salary_fi] 18 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_ie] reconciliation cache hit (data\raw\_reconcile_cache\salary_ie.json, <14d old)
    [salary_ie] 4 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_ie] live 'updated' stamp 2023-10-18T11:00:00.000Z matches the committed one; reconciled at year 2022
    [salary_es] reconciliation cache hit (data\raw\_reconcile_cache\salary_es.json, <14d old)
    [salary_es] 12 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_de] credential: DESTATIS_TOKEN (from prompts/run-package-11.cmd — package 11's own explicit, one-time exception to the environment-only rule; the token itself is never logged, only this label)
    [salary_de] reconciliation cache hit (data\raw\_reconcile_cache\salary_de.json, <14d old)
    [salary_de] 62361-0030: live period 2022-04..2025-04, committed reference_month=2025-04 — latest, matches
    [salary_de] 62361-0034: live period 2022..2025, committed year=2025 — latest, matches
    [salary_de] 2 live table(s) reconciled for structure and vintage (values not compared — see this function's docstring)
    [salary_uk] reconciliation cache hit (data\raw\_reconcile_cache\salary_uk.json, <14d old)
    [salary_uk] 1 live cell(s) suppressed or non-numeric in the workbook — excluded from comparison rather than compared as zero
    [salary_uk] 13 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_au] reconciliation cache hit (data\raw\_reconcile_cache\salary_au.json, <14d old)
    [salary_au] 14 field(s) reconciled against the live source, 0 mismatch(es)
    [salary_ca] reconciliation cache hit (data\raw\_reconcile_cache\salary_ca.json, <14d old)
    [salary_ca] 12 field(s) reconciled against the live source, 0 mismatch(es)
· Part C — derived chain reproduced by independent arithmetic (>=3 countries)
    [DK chain] (393.81 - 49.23 - 7.55) x 160.3333h/mo x 12 = 648445.72 — matches stored 648445.72
    [SE chain] 53500.0 x 12 = 642000.0 — matches stored 642000.0
    [SE chain] 642000.0 / 9.82070570950185 = 65372.08 — matches stored 65372.08
    [NL chain] 34.8 x 39.1h/wk x 52 = 70755.36 — matches stored 70755.35999999999
· Part D — every USD-converted figure uses its own native year's FX rate (validate_data.py, reused)
· normalise.py never substitutes a different year's FX rate than the one requested
  (15 committed wage_distribution.json rows scanned for FX-year mismatch: 0 found)
  0 violation(s)
· Part E — every annualised hour-basis figure has a sourced hours record for its own country/year
  5 hour-basis country row(s) checked, 0 without a sourced hours record

2 flag(s):
  ! salary_de: structure and vintage reconciled live, but the wage VALUES were NOT compared. GENESIS's data/table serves all 1,300 occupation rows and timed out at 90/150/180/300/600s this session (narrowing via classifyingvariable1=KB10A5 does not reduce its build time, and the async job service is refused for this token, Code 12); each abandoned attempt also holds one of the account's 3 parallel slots for 15 minutes, which would block src_salary_de.py's own harvest. Germany is NOT counted among the value-reconciled sources.
  ! bls_oews: live reconciliation SKIPPED — no BLS_API_KEY in the environment. BLS's unregistered OEWS API caps at 25 requests/day and src_bls_oews.py spends 9-11 of them per run, so no request was attempted rather than risking the harvester's own quota (NEEDS-DECISION.md #13). Not counted as reconciled.

PASSED — every reconciled figure matches its live source and independent recomputation.
```
</details>

---

## Snapshot — Tier 3, drift + coverage

`python scripts/snapshot_stats.py` — PASSED, clean

<details><summary>Full log</summary>

```
CS Migration Compass — drift and coverage snapshot (Tier 3)

· drift — comparing against the immediately previous snapshot
  52 source(s) compared, 0 material movement(s) flagged so far
  snapshot appended to data\quality_history\snapshots.jsonl (4 total)

· coverage matrix — every cell read from the resolver's own committed output
  15 countries in wage_distribution.json; 2 personalise by experience (['NO', 'SE'])
  AE        depth=1    comparable=True  bases=[none] experience=False postings=73 stated_pay=1.4%
  AU        depth=4    comparable=True  bases=[native_total_earnings, usd_total_earnings] experience=False postings=141 stated_pay=11.3%
  CA-21231  depth=4    comparable=True  bases=[none] experience=False postings=376 stated_pay=19.1%
  CA-21232  depth=4    comparable=True  bases=[none] experience=False postings=376 stated_pay=19.1%
  DE        depth=2    comparable=True  bases=[native_regular_pay, native_total_earnings, usd_regular_pay, usd_total_earnings] experience=False postings=298 stated_pay=21.1%
  DK        depth=4    comparable=True  bases=[native_regular_pay, native_total_earnings, usd_regular_pay, usd_total_earnings] experience=False postings=5 stated_pay=0.0%
  ES        depth=2    comparable=True  bases=[native_total_earnings, usd_total_earnings] experience=False postings=213 stated_pay=11.7%
  FI        depth=4    comparable=True  bases=[native_regular_pay, native_total_earnings, usd_regular_pay, usd_total_earnings] experience=False postings=7 stated_pay=0.0%
  GB        depth=4    comparable=True  bases=[native_total_earnings, usd_total_earnings] experience=False postings=696 stated_pay=20.0%
  IE        depth=1    comparable=True  bases=[native_regular_pay, usd_regular_pay] experience=False postings=69 stated_pay=13.0%
  NL        depth=None comparable=False bases=[native_regular_pay, usd_regular_pay] experience=False postings=138 stated_pay=12.3%
  NO        depth=4    comparable=True  bases=[native_regular_pay, native_total_earnings, usd_regular_pay, usd_total_earnings] experience=True postings=11 stated_pay=0.0%
  QA        depth=1    comparable=True  bases=[none] experience=False postings=5 stated_pay=40.0%
  SE        depth=4    comparable=True  bases=[native_regular_pay, usd_regular_pay] experience=True postings=106 stated_pay=3.8%
  US        depth=4    comparable=True  bases=[native_regular_pay, usd_regular_pay] experience=False postings=11208 stated_pay=61.0%
  coverage matrix written to data\quality_history\coverage_matrix.json

no material drift since the previous snapshot.
```
</details>

---

See [`docs/REGRESSION-CATALOGUE.md`](REGRESSION-CATALOGUE.md) for the historical bugs each check exists to catch, and [`data/quality_history/`](../data/quality_history/) for drift snapshots over time.
