# CS Migration Compass — data pipeline dependencies
# Pinned loosely on purpose: these are all stable, widely-packaged libraries and
# the pipeline is re-runnable from scratch with `make pipeline`.

requests>=2.31          # all HTTP (browser UA set in scripts/_common.py)
pandas>=2.0             # CSV/SDMX frame handling (FHFA, UK HPI, Indeed, RSF, SO survey)
openpyxl>=3.1           # xlsx readers (UN DESA, WHR, MIPEX, IMF WEO)
beautifulsoup4>=4.12    # HTML parsing (Wikipedia English speakers, Numbeo)
lxml>=5.0               # fast HTML/XML backend for bs4 + pandas.read_html
pdfplumber>=0.11        # PDF text extraction (EF EPI, WIPO GII)
truststore>=0.9         # use the OS certificate store — housepriceindex.ca (Teranet)
                        # serves an intermediate that certifi does not carry
Pillow>=10.0            # renders the Open Graph link-preview images
                        # (scripts/generate_og_images.py, run by `npm run build`)
PyYAML>=6.0             # scripts/postings_classify_config.yaml (package 12, tier 3) — every
                        # user-facing classifier filter lives there, never hardcoded in the .py

# --- package 15: statistical audit ---
# Version floors are the exact versions package 15's own analysis ran against
# (recorded so a future re-run can tell "the stats moved" from "the library
# moved"). scipy: distribution fits, Shapiro-Wilk/Anderson-Darling, bootstrap.
# scikit-learn: IsolationForest/LOF, TF-IDF + linear title classifier,
# gradient-boosted model-based record validation. statsmodels: OLS/Deming
# support, VIF, CUSUM/Chow structural-break tests.
scipy>=1.15.1           # 1.15.1 used by package 15
scikit-learn>=1.8.0     # 1.8.0 used by package 15
statsmodels>=0.14.6     # 0.14.6 used by package 15
