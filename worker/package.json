{
  "name": "cs-compass-cv-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker: CV text in, a structured career profile out. Never a pay figure.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/**/*.test.ts",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.104.0"
  }
}
